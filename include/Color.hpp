#pragma once

// Note: Colors BLACK and WHITE are reserved for special uses
// and colors from RED to CYAN are assigned to users.

enum class Color {
  BLACK = 1,
  RED,
  GREEN,
  YELLOW,
  BLUE,
  MAGENTA,
  CYAN,
  WHITE,
};
